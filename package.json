{
  "name": "biblioteca-projeto",
  "version": "1.0.0",
  "description": "Script raiz para iniciar API e Frontend juntos",
  "scripts": {
    "dev": "concurrently -n API,FRONT -c blue,green \"npm run start --prefix api\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
